services:
  app:
    env_file:
      - .env
    build: .
    ports:
      - "${APP_ADDR}:8081"
    depends_on:
      - redis
    volumes:
      - ./data:/data
  redis:
    restart: always
    image: redis:7-alpine