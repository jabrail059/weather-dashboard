services:
  app:
    env_file:
      - .env
    build: .
    ports:
      - "${APP_PORT}:8081"
    depends_on:
      - redis
    volumes:
      - ./data:/data
  redis:
    restart: always
    image: redis:7-alpine